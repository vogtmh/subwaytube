import { Innertube, UniversalCache } from 'youtubei.js';

// Export Innertube to the global scope so it can be used in the UWP app
window.Innertube = Innertube;
window.UniversalCache = UniversalCache;
